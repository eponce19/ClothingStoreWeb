@model Udem.LlamaClothingCo.Entities.Item
 
@{
    ViewBag.Title = "Browse Products";
}
 
<h2>Your cart</h2>
<table class="responsive" text-align: center">
    <thead>
        <tr>
            <th style="text-align: center">Quantity</th>
            <th style="text-align: left">Item</th>
            <th style="text-align: right">Price</th>
            <th style="text-align: right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
         <tr>
                <td style="text-align: center">Model.Quantity</td>
                <td style="text-align: left">Model.Name</td>
                <td style="text-align: right">Model.Cost.ToString("c")</td>
                <td style="text-align: right">Model.Cost.ToString("c"))
                </td>
                <td>
            @*<tr>
                <td style="text-align: center">1</td>
                <td style="text-align: left">@Model.Name</td>
                <td style="text-align: right">@Model.Cost.ToString("c")</td>
                <td style="text-align: right">1 * @Model.Cost.ToString("c"))
                </td>
                <td>*@
                   @* @using (Html.BeginForm("RemoveFromCart", "Cart"))
                    {
                        @Html.Hidden("ItemId", @Model.Id)
                        @Html.Hidden("ReturnUrl", Model.ReturnUrl)
                        <input type="submit" class="actionButtons" value="Remove" />
                    }*@
                </td>
            </tr>
       @* }*@
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" style="text-align: right">Total:</td>
            <td style="text-align: right">
                @*@Model.Cart.ComputeTotalValue().ToString("c")
            </td>*@
        </tr>
    </tfoot>
</table>
<p style="text-align: center" class="actionButtons">
    <a href="ReturnUrl" >Continue shopping</a>
    @Html.ActionLink("Checkout now", "Checkout")
</p>

