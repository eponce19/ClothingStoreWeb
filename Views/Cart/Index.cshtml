@model IEnumerable<Udem.LlamaClothingCo.Entities.Sale>
 
@{
    ViewBag.Title = "Browse Products";
}
 
<h2>Your cart</h2>
<table class="responsive" text-align: center">
    <thead>
        <tr>
            <th style="text-align: center">Quantity</th>
            <th style="text-align: left">Item</th>
            <th style="text-align: right">Price</th>
            <th style="text-align: right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
       @* @foreach (var line in Model.Cart.Lines)
        {
            *@<tr>
                <td style="text-align: center">Quantity</td>
                <td style="text-align: left">Product.Name</td>
                <td style="text-align: right">Product.Price.ToString("c")</td>
                <td style="text-align: right">Quantity * line.Product.Price).ToString("c"))
                </td>
                <td>
                    @*@using (Html.BeginForm("RemoveFromCart", "Cart"))
                    {
                        @Html.Hidden("ProductId", line.Product.ProductID)
                        @Html.Hidden("ReturnUrl", Model.ReturnUrl)
                        *@<input type="submit" class="actionButtons" value="Remove" />
                    }
                </td>
            </tr>
       @* }*@
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" style="text-align: right">Total:</td>
            <td style="text-align: right">
                @*@Model.Cart.ComputeTotalValue().ToString("c")
            </td>*@
        </tr>
    </tfoot>
</table>
<p style="text-align: center" class="actionButtons">
    <a href="ReturnUrl" >Continue shopping</a>
    @Html.ActionLink("Checkout now", "Checkout")
</p>

