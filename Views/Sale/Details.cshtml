@model IEnumerable<Udem.LlamaClothingCo.Entities.SaleDetail>
@{
    ViewBag.Title = "Browse Sale Detail";
}
 
<h2>Your Orders</h2>
<div>
<table class="responsive" text-align: center">
   <thead>
       <tr>
           <th style="text-align: center">Item Id</th>
           <th style="text-align: left">Quantity</th>
       </tr>
   </thead>
  <tbody>
        @foreach (var saledetail in Model)
        {
            <tr>
                <td style="text-align: center">@saledetail.ItemId</td>
                <td style="text-align: left">@saledetail.Quantity</td>
                <td>
                    @*@using (Html.BeginForm("RemoveFromSales", "Sale"))
                    {
                        @Html.Hidden("sale", sale.SaleId)
                        <input type="submit" class="actionButtons" value="Remove" />
                    }*@
                </td>
            </tr>
        }
    </tbody>
</table>

    </div>